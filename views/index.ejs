<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%= title %> 
    <ul>
        <% messages.forEach(function(message) { %>
            <h2><%= message.user %></h2>
            <li><%= message.text %></li>
            <li><%= message.added %></li>
        <% }); %>
    </ul>
    <a href="http://localhost:3000/new"><button>add</button></a>
</body>
</html>